# История изменений харнеса Personal OS

Полный журнал. Вынесен из `CLAUDE.md`, чтобы не занимать контекст каждой сессии.
Читать выборочно: `grep -n "ключевое слово" docs/harness-changelog.md`, затем `sed -n "N,Mp"`.

| Дата | Изменение | Объект | Причина |
|------|-----------|--------|---------|
| 2026-05-31 | Начальная сборка | Весь харнес | Первичная настройка |
| 2026-05-31 | Psych Team | agents/cognitive,emotional,relational-analyst, lead-psychologist, skills/psych-analysis/ | Психологический анализ |
| 2026-05-31 | Startup Lab | agents/market-researcher, product-manager, growth-marketer, smm-specialist, business-psychologist, skills/startup-lab/ | Продукты, маркетинг, трекинг |
| 2026-05-31 | NotebookLM везде | все agents/*.md, skills/*/SKILL.md | 10–50 источников, ✅/⚠️/❓ |
| 2026-05-31 | Система контекстов | skills/sync-claude-sessions/, skills/agent-context/ | Контексты агентов в Obsidian |
| 2026-05-31 | hr-specialist | agents/hr-specialist.md | Личный HR + HR стартапа + hh.ru |
| 2026-05-31 | Dev Lab (10 агентов) | agents/tech-lead,frontend,backend,ui-web,ui-mobile,ux-researcher,ux-architect,code-reviewer,design-reviewer,tech-writer, skills/dev-lab/ | Полный цикл разработки |
| 2026-05-31 | Agent Teams + самооптимизация + мастер-оркестратор | все файлы | Agent Teams, Obsidian как общая KB, самооптимизация, кросс-команды, единый personal-os |
| 2026-05-31 | Psych Team расширение: social-psychologist, blind-spot-analyst (batch+interactive), sexologist, prognosis-analyst | agents/social-psychologist,blind-spot-analyst,sexologist,prognosis-analyst.md, skills/psych-analysis/SKILL.md | Социальная динамика, самообман (диалог), сексология, карта трансформации |
| 2026-05-31 | Протокол сессий: вербатим под копирку | skills/psych-analysis/SKILL.md, blind-spot-analyst | Сохранение полных диалогов без summary |
| 2026-05-31 | Workspace → Obsidian | все SKILL.md, CLAUDE.md, workspace-paths.md | Рабочее пространство всех команд теперь в Obsidian 10 — Claude/Рабочее пространство/ |
| 2026-05-31 | Авто-загрузка контекста | psych/startup/dev-lab SKILL.md | Убран вопрос "загрузить контекст?" — теперь автоматически |
| 2026-05-31 | lead-psychologist оптимизация | agents/lead-psychologist.md | Читает гипотезы-файлы первыми для экономии контекста |
| 2026-06-11 | Системные команды brainstorm + update-memory | skills/personal-os/SKILL.md | Выявление новых семантических связей + полный цикл L-памяти (L0→L3) с переиндексацией и перестройкой графа |
| 2026-06-11 | Фикс путей vsearch | skills/personal-os/SKILL.md, agents/vault-indexer.md | Верный путь tools/obsidian/tools/ вместо битого obsidian/tools/ |
| 2026-06-12 | brainstorm + update-memory как отдельные скиллы | skills/brainstorm/, skills/update-memory/ | Команды вызываются напрямую `/brainstorm`, `/update-memory` |
| 2026-06-12 | README со всеми командами | skills/personal-os/README.md | Единый справочник всех команд системы |
| 2026-06-12 | _workspace → Obsidian везде (60 файлов) | все agents/*.md, skills/*/SKILL.md+references, CLAUDE.md | Рабочее пространство всех агентов/команд только в `10 — Claude/Рабочее пространство/{команда}/`; добавлены research/office/tutor |
| 2026-06-21 | Семантический граф → L4-память | memory-extractor.md (Шаг 5 L4), vault-indexer.md, skills/brainstorm, skills/update-memory, skills/personal-os (SKILL+README), Memory/README.md, CLAUDE.md; перенос `Semantic Graph Log.md` → `Memory/L4 Semantic Graph/` | Граф стал отдельным ассоциативным уровнем памяти L4 в 5-уровневой модели L0→L1→L2→L3→L4; всё, связанное с графом, ведёт в L4 |
| 2026-06-24 | Скилл disk-cleanup | skills/disk-cleanup/SKILL.md, CLAUDE.md (таблица скиллов) | Зафиксирована процедура очистки места на диске macOS (3 зоны риска: 🟢 кэши / 🟡 переустанавливаемое / 🔴 личные данные) |
| 2026-06-25 | Идиолект-профиль повадок в memory-extractor | agents/memory-extractor.md (теги [INSIGHT]/[BEHAVIOR]/[VOICE] в L1, Шаг 4b), skills/update-memory/SKILL.md, skills/personal-os/SKILL.md; новый файл памяти `10 — Claude/Memory/L3 Persona/Идиолект и повадки.md` | memory-extractor теперь ловит мысли/инсайты, поведение и манеру письма Димитри (вплоть до мелочей, с цитатами и датами) из его реплик в L0 и ведёт отдельный растущий профиль в Obsidian |
| 2026-06-26 | lecturer: построчная атомизация кода | agents/lecturer.md (новый раздел «Программный материал»: построчный разбор, атомарные заметки-понятия по синтаксису с антидублем через obsidian_search/vsearch в `tutor/concepts/{язык}/`, разбор взаимодействия кода + паттерны + конвенции) | По запросу: для обучения программированию лектор дробит каждую строку кода на синтаксические понятия (отдельный файл на понятие, если ещё нет) и детально объясняет, как код работает вместе, какие паттерны и конвенции используются |
| 2026-06-26 | exercise-designer: упражнения на закрепление кода | agents/exercise-designer.md (новый раздел «Программный материал»: дриллы по понятию, трассировка, поиск ошибок, применение паттернов/конвенций; правило покрытия — на каждое понятие/разбор своё упражнение; интерливинг + связи `[[...]]` в заметки-понятия, передача в learning-coach для интервального повторения) | По запросу: агенты должны делать задания и упражнения, закрепляющие атомизированный материал по коду |
| 2026-06-26 | Tutor Lab: аудит пробелов базы знаний | skills/pedagogy/references/knowledge-gap-audit.md (новый метод: сигналы заглушка/сирота/висячая ссылка/MOC-без-подложки/перекос/устарело + команды vsearch+driver + отчёт `tutor/knowledge_gaps.md`), skills/pedagogy/SKILL.md (§9), agents/learning-needs-analyst.md (режим «что проработать»), curriculum-architect.md (аудит покрытия, статус модулей ✅/⚠️/❌), learning-coach.md (мониторинг пробелов по ходу) | По запросу: тюторы подсказывают, какие темы раздела «05 — Знания и навыки» раскрыты слабо, чтобы их проработать |
| 2026-07-04 | Скилл disk-sync | `~/.claude/skills/disk-sync/SKILL.md` + `scripts/resync.sh`, CLAUDE.md (таблица скиллов) | Синхронизация Яндекс.Диска на внешний ЖД. Зафиксированы грабли: macOS rsync = openrsync (нет `--info/--no-perms`, только `-rlt --modify-window=1`); повреждённые каталоги NTFS (Tuxera) роняют диск под нагрузкой → копирование по-каталожно с изоляцией сбоев + исключение `.git/` по умолчанию; добавляющая синхр. без удаления (бережём уникальное на диске: `Work/`, case-дубли); финальная сверка через `comm`; ремонт битого NTFS — `chkdsk /f` / Tuxera Repair |
| 2026-07-03 | Векторизация библиотеки Zotero | `tools/zotero/vectorize.py` (новый: таблицы books/annotations/chunks, эмбеддинги MiniLM как у Obsidian → единое смысловое пространство; команды index / index-chunks / ocr-scans / search / search-all; numpy-кэш `_*.npy` для быстрого поиска ~0.1с; детект сканов + OCR через tesseract rus+eng), `tools/zotero/SKILL.md` (секция семантического поиска + фикс пути к реальной базе на Yandex.Disk), agents knowledge-agent/deep-researcher/pattern-finder/academic-writer (провод поиска: своя библиотека первой, кросс-поиск книги↔заметки, ГОСТ-цитаты со страницей), `.gitignore` (деривативы) | Запрос: поднять локальный доступ к книгам и векторизовать их в базу; 757 выделений + полный текст 123 книг (~83k чанков) как ещё один уровень памяти Personal OS. Реальная библиотека Zotero — на Yandex.Disk (`Self-Education/Knowledge base/Zotero`), не в `~/Zotero`; тулам нужен `ZOTERO_DATA_DIR` |
| 2026-07-04 | Хранилище визуальных референсов (борды-мудборды) | `tools/context-store/store.py`+README (движок бордов на stdlib: полярность Нравится/Не нравится, add-url/add-file/add-b64/ingest-pinterest/manifest, TLS-fallback truststore→certifi→unverified, slug кириллицей), данные в Obsidian `08 — Шаблоны и ресурсы/Визуальные референсы/` (board.json + board.md с галереями и `## Визуальное ДНК`), mission-control `server.py` (роуты `/api/refs*`, `/refs/img`; UTF-8-форс подпроцесса) + `index.html` (раздел «Хранилище контекстов» с разделителем **Графы памяти / Изображения**: галерея бордов, добавление ссылками/Pinterest-пачкой/загрузкой, кнопка «визуальное ДНК» → Claude-роут), `ui-designer-web`+`ui-designer-mobile` (читают борды перед выбором стиля: Нравится→do's, Не нравится→don'ts) | Запрос: единый способ передавать желаемый визуальный контекст в дизайн. Хранилище контекстов объединяет графы памяти и визуальные борды. Урок: mission-control работает под пользователем — данные, созданные из root-шелла, роняют запись сервера (EPERM) → chown на пользователя; ошибка сервера была не в кодировке, а в правах |
| 2026-07-11 | Пакеты скиллов для Claude Desktop (Cowork) | `install/*.skill` (16 пакетов — все скиллы из `.claude/skills/`), CLAUDE.md (раздел «Установка в Claude Desktop») | Запрос: перенести команды системы в десктоп. Скиллы упакованы в `.skill` (zip содержимого папки скилла) и установлены в Cowork. Нюанс Cowork: bash — в песочнице, драйверы `~/.claude/skills/` и vault недоступны без подключения папок в сессию |
| 2026-07-04 | Zotero: фоновый сервис + граф библиотеки + mission-control | `services/zotero-index/indexer.py` (новый демон под pos: инкрементальный `refresh` подпроцессом каждые 6ч, только при изменениях, пишет `status.json`), `services.json` (сервис zotero-index), `vectorize.py` (команды `graph` — карта книг по смысловой близости 90 узлов/381 ребро, переиспользует раскладку+HTML графа Obsidian с легендой по типам книг; `refresh` — умный инкремент, перестройка графа и по изменению числа чанков; WAL+busy_timeout против блокировок), mission-control `server.py`+`index.html` (роут «Библиотека · Zotero»: `/api/zotero`, `/zotero/graph`, `/api/zotero/rebuild` + карточка со статусом и графом) | Запрос: поднять zotero как фоновый сервис к остальной инфраструктуре, сверстать граф библиотеки как у заметок и вывести в mission-control. OCR 2 сканов (Java в облаке +1784, Head First GIT +1136 чанков → 85 638). Урок: sqlite фейлил `database is locked` при чтении дашбордом во время записи → перевод вектор-БД в WAL |
| 2026-07-14 | Siri → desktop-avatar (голосовой вход) | `apps/desktop-avatar`: новый `SiriEndpoint.swift` (мини-HTTP `127.0.0.1:8788/ask`, только loopback, порт — `AVATAR_SIRI_PORT`; GET `?text=` и POST JSON), `AvatarStateController.chat(completion:)` (ответ мозга уходит и в пузырь, и в шорткат; агентские задачи отвечают сразу «работаю» — против таймаута Shortcuts), README (раздел «Голос через Siri», статус) | Запрос: объединить Siri и аватара. Siri = микрофон+динамик (шорткат: диктовка → `/ask` → «Озвучить текст»), мозг — ChatBrain/AgentBrain аватара. Урок: очередь ChatBrain серийная — один запрос, упёршийся в 60с таймаут сокета claude-local-api, задерживает все последующие (симптом «эндпоинт завис», на деле ждёт очередь) |
| 2026-07-19 | Смысловой слой в автоотклике hh (по мотивам fikstt2/hh-ai-agent) | `services/hh-autoapply/`: новые `llm.py` (клиент к claude-local-api по unix-сокету, мягкая деградация), `analyzer.py` (отсев вакансий моделью 0–10 батчами + персональное письмо под вакансию с санитарной проверкой), `history.py` (SQLite `history.db` вместо плоского applied.json, миграция 226 id, зеркалирование в applied.json для mission-control), `hhdriver.py` (общая обвязка драйвера), `responses.py` (монитор приглашений/отказов HR) + переписан `autoapply.py` (конвейер: фильтр названий → описания → LLM-отсев → письмо → отклик; флаги `--dry/--explain/--no-llm`), `config.json` (блок `llm` с порогом и критериями), README сервиса; крон-задача `hh-responses` (10:30 и 20:30) | Запрос: улучшить автоотклик наработками из репозитория. Взято: LLM-фильтрация по описанию, генерация писем, БД истории, мониторинг ответов HR. Переложено на свою инфраструктуру (claude-local-api вместо Ollama, общий `tools/hh/driver.mjs` вместо своего Playwright, приёмники крона вместо своего TG-бота). Отличия от оригинала: оценка батчами (~9с на запрос модели) и шкала 0–10 с обоснованием вместо YES/NO. Урок: hh под VPN отдаёт заглушку «VPN мешает работе сайта» с кодом 200 — она успела закэшироваться как «профиль кандидата», добавлена проверка на страницы-заглушки |
| 2026-07-19 | Пульт автоотклика hh в личном ассистенте (`/hh`) | `services/tg-psych-bot`: новые `app/integrations/hh_agent.py` (мост к hh-autoapply: чтение history.db в режиме ro, правка config.json, пауза через `enabled` крон-задач, разовый прогон в subprocess) и `app/handlers/hh.py` (карточка статуса + кнопки: прогнать/dry-run/пауза/порог/правки стиля письма + `push_invitations`), регистрация роутера в `app/bot.py`, вызов `_push_hh()` из тика планировщика; в hh-autoapply — колонки `kind`/`announced` в `negotiations`, `llm.letter_notes` в промпте письма | Запрос: подключить бота автоотклика к ассистенту. Выбран tg-psych-bot (один бот на всё личное), режимы: уведомления о приглашениях, управление прогоном, правки стиля письма. Карточек «подтверди каждый отклик» намеренно нет — при 10 откликах в день это 10 подтверждений, контроль вынесен в порог отбора и правки стиля. Архитектура: браузерную работу делает крон, бот только читает sqlite → не блокирует event loop и не зависит от доступности hh. Урок: индексы в SCHEMA по новым колонкам роняют `executescript` на старой базе — создавать их после ALTER-миграций |
| 2026-07-21 | Полный экспорт Telegram-диалогов с медиа | `~/.claude/skills/telegram/driver.cjs` (новая команда `export_chat`: пагинация всей истории через `iterMessages(reverse)`, скачивание фото/голосовых/кружков/видео/стикеров/документов в `media/{id}_{тип}.{ext}`, markdown в формате остальных выгрузок с `![](media/…)`-встраиванием, идемпотентность — скачанное не тянется повторно, лимит `max_media_mb`) + `telegram/SKILL.md`; проводка в команды: `skills/update-memory` (Шаг 0.5 — одна команда вместо ручной пагинации + правило «имя чата ≠ имя человека»), `skills/personal-os` (Шаг 0.5), `skills/psych-analysis` (п.4 — весь диалог для значимых отношений), `agents/action-agent` (принцип 1a + спрашивать перед выгрузкой личной переписки); перепакованы `install/{update-memory,personal-os,psych-analysis}.skill` | Запрос: сохранять переписку целиком, включая голосовые и кружки, — старый `get_chat_history` отдавал только текст. Заодно синхронизированы разошедшиеся копии скиллов: репозиторий отставал от `~/.claude/skills/` на блок про тег `ai-generated` |
| 2026-07-26 | Метрики авторства/софт-скиллов в tg-psych-bot (по плану психокоманды) | `services/tg-psych-bot`: новые слоты `agency` (ежедневная развилка «решил сам/подчинился/поспорил-назло/нет» + адресат + база Фаза 0) и `deed` (событийный лог поступка A/B/C), обзоры `weekly` (3 вопроса) и `monthly` (7 фактов да/нет по 7 целям) — тик-модель планировщика с guard, устойчивая к сну; новые `handlers/{agency,deed,review}.py`, `keyboards/agency.py`, метрики в `domain/metrics.py`, рендер секций в `note_body.py`, rolling-файлы обзоров `02 — Внутренний мир/Обзоры авторства/*` через outbox; кнопки меню «🧭 Развилка»/«📌 Поступок» + `/fork /deed /week /month`; вечерний чек-ин предлагает развилку одной кнопкой; спека `docs/agency-metrics-spec.md`; +10 тестов (81 зелёный) | Запрос: собрать спеку и внести в бота метрики ДЕЙСТВИЙ (не ярлыков личности) по мерке Димитри «решил сам · было страшно · сделал». Жёсткие правила соблюдены в коде: новые слоты вне heatmap-интенсивности → без стрик-давления; никаких баллов/сумм/вердиктов; «развилки не было» и пустые события — не провалы; дневник состояний (состояние) и агентность не смешиваются в общий балл |
| 2026-07-26 | База источников: граф по origin, поллер, карточки mission-control, backfill ProLab120, импорт блога + фикс TG-драйвера | `tools/zotero/vectorize.py` (`cmd_build_graph(color_by, outdir)` + `ORIGIN_COLORS/NAMES` — граф раскрашивается по origin, Zotero-граф без изменений); `tools/sources/ingest.py` (команда `graph` → `tools/sources/graph/`, устойчивый парсер `_first_json` по наибольшему концу JSON); новый сервис `services/sources-index/` (indexer.py+config.json, под pos, 6ч: идемпотентный забор ProLab120 + пересборка графа + status.json) в `services.json`; mission-control `server.py` (`/api/sources`,`/sources/graph`,`/api/sources/rebuild`,`/api/blogbase` + `sources_meta/sources_rebuild/blogbase_admin`) + `index.html` (3-я карточка «База источников» в «Графы памяти», route `sources` с iframe+темой, admin-карточки «Источники · база» и «Блог · база»); `tools/blog/store.py` (тот же фикс парсера); **фикс `~/.claude/skills/telegram/driver.cjs`** — gramjs слал INFO-логи в stdout и асинхронно интерливил их с итоговым JSON (при limit 500+ потребитель получал огрызок/один объект); увёл весь `console.log`→stderr, результат печатает `emit()` в stdout и `await`-ит запись перед `process.exit` (иначе большой ответ обрезался); данные: backfill ProLab120 (10000 сообщений → 440 ссылок → 334 community-источника, граф 424 узла/1827 рёбер), импорт @digit_code в Base B (70 статей) | Запрос: добавить граф базы источников в контекстное хранилище и доделать остаток (полный backfill, постоянный забор, карточки, импорт блога). Урок вынесен в память `telegram-driver-stdout-interleave`: симптом «N сообщений = 4/5» = парсер поймал словарь вместо списка из-за интерлива логов; `client.setLogLevel('none')` в gramjs 2.26.21 не работает |
| 2026-07-19 | Дробление L-памяти по дням (борьба с расходом токенов) | Vault `10 — Claude/Memory/`: L4 `Semantic Graph Log.md` → тонкий индекс + файлы-дни `Log/Graph Log {дата}.md`; все сцены L2 → тонкие ядра (0.6–3.4KB вместо 8–76KB) + файлы-дни `{сцена}/{сцена} {дата}.md`; L3 `persona.md` 71→15KB (модули `Психологический профиль.md`, `Паттерны поведения.md` отдельно), `Идиолект и повадки.md` 57→2KB (архивы `Идиолект/Идиолект — *.md` + файлы-дни `идиолект {дата}.md`); правила чтения/записи — agents/memory-extractor.md (раздел «Бюджет токенов»), vault-indexer.md, skills brainstorm/update-memory/personal-os (+README), Memory/README.md; install/*.skill перепакованы | Прогон update-memory съедал десятки тысяч токенов: накопительные файлы (164KB лог L4, 120KB persona) читались целиком и дописывались в конец. Теперь агенты читают только тонкие ядра ≤ ~15KB и файл-день, пишут новыми файлами-днями. Имена файлов-дней с префиксом сцены — от коллизий wiki-ссылок с L1/дневниками |
| 2026-08-04 | Социальный капитал: база людей и связей | `tools/social-capital/` (новый: `capital.py` — SQLite `social_capital.db` со схемой contacts/orgs/channels/attrs/relations/interactions/categories, CRUD + `log`/`link` + `list --due` + `export-csv` + `import-telegram`, двусторонняя синхронизация с Obsidian `sync`/`pull`, граф `graph`; README); Obsidian раздел `08 — Социальный капитал/` (карточки `Люди/{Имя}.md` с русскими свойствами, `MOC — 08 — Социальный капитал.md`, таблица `Контакты.base` для плагина Bases, шаблон карточки); `tools/obsidian/tools/obsidian_vsearch.py` (секция «11» в SECTION_COLORS/NAMES — контакты видны в L4-графе своим цветом); mission-control `server.py` (`/api/capital`, `/capital/graph`, `/api/capital/rebuild`) + `index.html` (маршрут `capital` + 4-я карточка в «Графы памяти»); `agents/knowledge-agent.md` (раздел «Люди: социальный капитал») | Запрос: папка личных контактов-карточек в Органоне + SQLite-база с графом (работа, интересы, почты, адреса, категории) как потенциальная база клиентов/связей. Решения: карточка = frontmatter (машинная часть, её же читает Bases) + авто-блок + свободный хвост ниже `<!-- /capital:auto -->`, который синхронизация не трогает → правки руками не теряются; рёбра графа не векторные, а фактические (явная связь, общая организация, общие интересы/навыки, общий город слабым штрихом), тег шире 12 человек в рёбра не идёт — иначе граф «все со всеми»; вид графа переиспользует движок графа заметок |
| 2026-08-04 | Соцкапитал: подкатегории, импорт не только из TG, перенумерация разделов Органона | `tools/social-capital/capital.py` (дерево категорий: колонка `parent`, `cat_root/cat_tree/cat_find/cat_ensure/cat_color/cat_label`, подкатегории по умолчанию для клиентов и лидов, `category add --parent` / `category rm --move-to`, срез `list --category` включает ветки, в карточке два свойства `категория`+`подкатегория`, в графе — своя строка легенды и оттенок родительского цвета; новые импорты `import-vcf` (vCard из Контактов macOS/iOS и Google) и `import-csv` (Google Contacts, LinkedIn, CRM, своя табличка) с идемпотентным дозаполнением `_upsert_imported`; `import-telegram` — отсев ботов и себя, `--contacts-only`, подтягивание телефона); `~/.claude/skills/telegram/driver.cjs` (в `get_dialogs` — `isBot/isContact/firstName/lastName/phone`); **перенумерация разделов vault**: `08 — Социальный капитал` (новый), Шаблоны 08→09, Архив 09→11, Claude остался 10 — правки в `tools/obsidian/driver.mjs` (+ глобальная копия), `obsidian_vsearch.py` (SECTION_COLORS/NAMES), `tools/obsidian/SKILL.md`, скиллах update-memory/personal-os/psych-analysis (репо + `~/.claude/skills`), `telegram/SKILL.md`, `obsidian/SKILL.md`, `services/tg-assistant` (config/notes), `mission-control/server.py` (REFS_ROOT + передача `CONTEXT_STORE_ROOT` в store.py, чтобы путь не дублировался), CLAUDE.md (таблица разделов), `agents/knowledge-agent.md`; `obsidian_vsearch.py cmd_index` — удаление из индекса путей, которых больше нет (после переезда разделов осталось бы 134 мёртвых записи) | Запрос: контакты бывают не только из Telegram, а лиды хочется делить на подкатегории; плюс раздел переехал на 08 и все пути сдвинулись. Уроки: sqlite `lower()` не понимает кириллицу — регистронезависимый поиск категорий делается в Python; имена файлов карточек надо санировать (ники из TG со слэшами роняют запись); часть файлов репозитория root-owned (правились из root-шелла) — записать в них из пользовательской сессии нельзя, нужен `chown` |
| 2026-08-06 | Рассыльщик ВК для заказчицы (первый платный внешний проект) | `apps/vk-poster/` (новое приложение: `run.py` — бот+веб+планировщик в одном процессе; `app/planner.py` — план на день, случайные паузы, дневной потолок, ночная тишина, остывание группы, ретраи и автопауза при капче/флуде; `app/vk.py` — VK API, классификация ошибок на «глушим группу» / «глушим страницу» / «повторим», парсер списков групп в формате `[club123|@name]`+ссылки+`@имена`; `app/web.py` — REST мини-аппа; `app/security.py` — проверка подписи Telegram initData; `app/crypto.py` — Fernet для токенов ВК; `webapp/` — мини-апп на ванильном JS, 5 вкладок; `deploy/` — systemd+Caddy; `docs/ТЗ.md` — требования дословно из переписки, `docs/BACKLOG.md`); тесты 24 зелёных; Obsidian: проект `06 — Проекты/Рассыльщик ВК — Russian Horse Market.md`, карточка заказчицы в соцкапитале (clients-active) | Запрос: прочитать диалог в TG с Hope и реализовать заказ. Продукт: рассылка объявлений по группам ВК с базой постов, несколькими страницами, расписанием и защитой от бана. Решения: мини-апп вместо чистого бота (база с редактированием кнопками неудобна), одно приложение в одном процессе (заказчику нужен VPS за 350 ₽, а не кластер), импорт списка групп «как есть» — в том виде, в каком заказчица его прислала. Урок: gramjs-драйвер не находит entity личного диалога по id, если он не в кэше, — сначала `getDialogs`, потом `getMessages(dialog.entity)` |
| 2026-08-06 | Рассыльщик ВК: только свои сообщества, охват, Excel, полный CLI | `apps/vk-poster/`: **убраны предложки** — публикация только `from_group=1` в сообщества, где страница админ (`groups.get(filter=admin)`), иначе явное «нужно быть администратором»; новые `app/faq.py` (справка — один источник для бота и мини-аппа), `app/excel.py` + `app/dataio.py` (выгрузка/загрузка базы в `.xlsx`, три листа, идемпотентный импорт по ссылке на запись ВК), `app/accounts.py` (подключение страницы — общее для бота и мини-аппа); `db.py` — таблицы `quiet_periods` (произвольные тихие окна вместо зашитой ночи) и `job_stats` (снимки охвата), миграции колонок + перенос старых `night_*`; `planner.py` — `in_quiet`/`quiet_ends_at` (тишина сдвигает публикацию, а не съедает), `refresh_stats` через `wall.getById` раз в 3 ч две недели; `bot.py` переписан — второй полноценный интерфейс (посты/сообщества/рассылки/журнал/настройки/тишина/экспорт-импорт командами и кнопками, FSM-мастера, текстовый sparkline), диплинки в мини-апп якорем адреса (`#preview_12`, `#job_7`, `#auth`); webapp — единая шкала отступов и высот контролов, чекбоксы для массовых действий над сообществами, превью поста как запись ВК, SVG-график охвата, FAQ; удалены vkid/callback-флоу, `parse_group_list`, импорт чужих списков, `post_as`; `README.md` переписан, `.env.example` → `example.env`; тесты 24 → 47 | Запрос Димитри: «никаких предложек, всё через мои сообщества где я админ, потому что посты должны быть от лица группы» + экспорт/импорт Excel, статистика охвата с графиком, гибкие периоды, UX и дизайн, FAQ, дубль функционала в CLI, удалить мёртвый код, документация. Урок: `dev.vk.com` не открывается через WebFetch — вызовы сверены с живым API токеном (groups.get(filter=admin) отдал 4 сообщества с `admin_level`/`wall`, wall.getById принял формат `-owner_post`). Все 4 сообщества имеют `wall=2` (ограниченная стена) — админу это не мешает, запись выходит сразу, не в предложку |
| 2026-08-07 | Рассыльщик ВК: наборы владеют постами, публикация в назначенное время, мобильный UI без системных контролов | `apps/vk-poster/app/db.py` (новая `group_list_posts` + перенос старых `campaign_posts` join'ами — `INSERT OR IGNORE` не глотает нарушение внешнего ключа и одна осиротевшая строка не давала бы запуститься; `jobs.manual`, `media.kind/orig_name`, `accounts.photo`, сняты `posts.has_price` и `groups.requires_price`); `planner.py` (посты берутся из набора, `_account_ready(manual=True)` игнорирует тихие периоды); `vk.py` (`upload_wall_doc` + `upload_attachment`, `MAX_ATTACHMENTS`, аватар в `whoami`); `web.py` (вложения одной ручкой с пофайловым отказом вместо падения всей пачки, `clean_filename`, `/api/jobs/{id}/reschedule`, `at` в `publish_now`, наборы в `/api/state`); `excel.py`+`dataio.py` (колонка «Наборы» вместо «С ценой» — структура «что куда» переживает выгрузку); **переписан `webapp/app.js`** (прошлая сессия оставила его на старой вёрстке — приложение падало на `#account-switch`); `styles.css`+`index.html` (объёмная округлая шапка, свои барабаны времени 24ч, контурные иконки вкладок, сетка фото как в ВК, скролл длинного текста); `bot.py` (наборы поста экраном в чате); `faq.py` переписан простым языком; тесты 47 → 58 | Запрос Димитри списком: разные наборы — разные посты, «непонятно когда какой пост выйдет и куда», убрать флажок цены, вложения (не только фото), AM/PM в тихих периодах, отступы, FAQ не выделять и упростить, адаптировать всё под телефон. Решения: набор стал связкой «площадки + тексты» (рассылка отвечает только за расписание) — это и есть ответ на «что куда»; назначенное вручную время не двигают тихие периоды, но паузы и лимит остаются. Уроки: aiohttp отдаёт нелатинские имена файлов в multipart процентами (`%D0%9F…`) — без `unquote` имя уехало бы во вложение ВК; тесты планирования падали не от кода, а от часа запуска (окно 00:00–23:59 в 23:5x пустое) → фикстура `midday` замораживает время; headless Chrome игнорирует `--window-size` для вьюпорта — размер телефона задаётся только через `Emulation.setDeviceMetricsOverride` |
| 2026-08-07 | Рассыльщик ВК: открытые стены, календарь, тема, островок-таббар | `apps/vk-poster/app/vk.py` (`groups.search` с досмотром через `groups.getById` — поиск отдаёт скупые поля и без досмотра не видно ни стены, ни прав; `can_post` с откатом на `wall==1`; `post_to_group(from_group=…)`), `planner.py` (`can_publish`/`publishes_as_group`: админ → `from_group=1` от имени сообщества, открытая стена → `from_group=0` от имени страницы, стена «только админам» → отказ, в предложку по-прежнему не пишем), `db.py` (`groups.can_post` + миграция), `web.py` (`/api/groups/search` с разделением на «свои / открытая стена / писать нельзя», приём не-админских площадок, `sent_count`/`last_sent_at` у поста), `bot.py` (`group_mode` вместо «прав нет — публикация невозможна»); мини-апп: кнопка темы в шапке с запоминанием выбора в localStorage (подсказка Telegram главнее только до первого нажатия), календарь произвольной даты вместо ленты из 14 фишек, фильтр постов «не выходили / вышли / все», нижние вкладки плавающим островком (стекло: размытие + грань + тень), запрет выделения и обводки на кнопках, отступ между метриками и кнопками журнала, выровнены поля лимитов страницы; FAQ и README переписаны под два режима публикации; тесты 58 → 61 | Восемь пунктов от Димитри после первого показа. Главное — разворот прежнего решения «только свои сообщества»: у заказчицы 22 чужие группы из исходного ТЗ, и теперь они работают, если стена открыта. Уроки: приложение не умирает по SIGTERM (aiogram ставит свой обработчик, штатное завершение виснет) — перезапуск только через `scripts/restart.sh` с `kill -9`, иначе новый процесс падает с «address already in use», а снаружи отвечает старый; адрес trycloudflare меняется при каждом старте туннеля, старый в `.env` протухает молча; при перезапуске бота Telegram какое-то время держит прошлый long-poll и сыплет `TelegramConflictError` — это не второй экземпляр, проверяется одиночным `getUpdates` при остановленном приложении |
| 2026-08-07 | Рассыльщик ВК: причина пустого плана, часть постов под рассылку, вложения и наборы в CLI бота | `apps/vk-poster/app/planner.py` (`PlanResult(created, reason)` — каждый тупик планирования называет себя: выключенные сообщества, пустой набор, остывание с оценкой «через N ч», выбранный лимит, кончившееся окно; `_why_no_groups` разделяет «набор пуст / всё выключено / писать некуда»), `db.py` (`campaign_post_pick` — сузить рассылку до части постов набора; пусто = весь набор), `web.py` (`post_ids` в сохранении и состоянии рассылки, `media` в `/api/jobs/{id}/stats`, `_add_groups`→`add_groups` как общая точка для бота, фоновый `backfill_photos` аватарок страниц), `webapp/app.js` (превью переписано под ленту ВК: текст над вложениями, свёртка длинного текста кнопкой «Показать полностью…» и прокрутка самой записи, а не текста внутри; та же карточка в журнале с фото и видео; `upload()` на XHR с полосой прогресса; «отметить/снять все» у сообществ и в листах выбора; фильтр постов по набору; авторастущее поле текста; причина пустого плана листом, а не тостом), `styles.css` (шапка сливается с фоном до прокрутки и в светлой теме; `:focus-visible` только при мыши — на телефоне он срабатывал от пальца и оставлял синюю рамку на FAQ, шапке и кнопке темы; отступы формы добавления сообществ), `bot.py` (paritet с мини-аппом: фильтр постов, приём вложений прямо в чат, публикация «сейчас/ДД.ММ ЧЧ:ММ», перенос записи из очереди, поиск и импорт списком сообществ, экран «какие посты крутить» и план на сегодня, сверка держит `can_post`), `faq.py`+`README.md`; тесты 66 → 73 | Итоговый список правок Димитри после второго показа. Главное по сути: «пересобрать день» врал — писал «планировать нечего», хотя причина была конкретная (все сообщества набора выключены), и разным расписаниям понадобилось крутить разные объявления одного набора. Уроки: аватарки страниц, подключённых до появления колонки `photo`, приходится дозаполнять — но только фоновой задачей, синхронный `whoami` в `/api/state` держал экран дольше 5 с; `datetime.strptime` без года подставляет 1900-й и спотыкается на 29 февраля — недостающее дописываем к строке, а не правим разобранную дату; адрес trycloudflare умирает молча (процесс жив, домен уже NXDOMAIN) — проверять `curl` по адресу, а не наличием процесса |
| 2026-08-07 | Рассыльщик ВК: остывание стало рекомендацией, вложения над текстом, фикс тапа по флажкам и крестику | `apps/vk-poster/app/planner.py` (остывание больше не выкидывает сообщество из плана — только двигает его в конец очереди; `PlanResult.warnings` несёт «ещё не остыли: …», план при этом собирается), `web.py`+`bot.py`+`app.js` (предупреждения показываются отдельно от отказа: план собран — лист с ⚠️, не собран — причина), `webapp/app.js` (в превью и журнале вложения снова над текстом, текст отбит от них отступом), `styles.css` (крестик удаления вложения переехал внутрь плитки с областью попадания 44 pt — снаружи его подрезала прокрутка ленты; `pointer-events: none` у флажков внутри `<label>`), `faq.py`, тексты про остывание в мини-аппе и боте | Три замечания Димитри + «удалить вложение не получается». Суть: «никаких таймаутов по 8 часов, когда захотел — тогда и сделал», остальное — предупреждение. Уроки: тап по `<input type=checkbox>` внутри `<label>` в WebKit срабатывает дважды (сам input + переброс от label) — галочка ставится и тут же снимается, лечится `pointer-events: none` на input; кнопка 22 pt, свисающая за край контейнера с `overflow-x: auto`, пальцем не нажимается — её подрезает прокрутка; такие баги ловятся только настоящим тапом через `Input.dispatchMouseEvent`, `element.click()` из консоли проходит всегда |
| 2026-08-09 | Хуки харнеса (Claude Code hooks) | `.claude/settings.json` (новый: SessionStart / UserPromptSubmit / PostToolUse Write\|Edit / SessionEnd), `.claude/hooks/session_start_status.py` (лежащие сервисы pos, упавшие крон-задачи, отставание вектор-индекса — молчит, когда всё в порядке), `.claude/hooks/session_end_export.py` (авто-экспорт сессии в L0 с явным `VAULT_DIR`), `.claude/hooks/post_write_vault.py` (очередь `.reindex-queue` + починка root-овского владельца файла), `.claude/hooks/risk_screen.py` + `risk_rules.json` (скрининг запроса на риск блокировки аккаунтов: TG/VK/hh, обход капчи, накрутка, Usage Policy Anthropic, локальный API наружу), `.gitignore` (очередь) | Запрос: «сделай хуки для себя по моей системе». Три вещи, которые раньше держались на памяти агента, стали механикой: состояние системы приезжает в контекст само, вербатим сессии уезжает в L0 без напоминания, правки заметок не теряются для индекса. Скрининг риска добавлен по требованию Димитри — предупреждать до необратимого действия наружу, не отказывая. Решения: раздел `08 — Социальный капитал` (4997 карточек, в индексе 7) исключён из метрики отставания — контакты живут в своей базе и графе, иначе хук кричал бы о 4990 «непроиндексированных»; блокирующих стоп-кранов на Bash сознательно нет (Димитри их не выбрал) — хуки только сообщают. Замеры: SessionStart 0.17 с на 5782 файла. Обнаружено попутно: `claude-sessions export` пишет не полный вербатим (7 реплик из 95 записей транскрипта) — вызовы инструментов и их результаты отбрасываются, сохраняется разговорный слой |
| 2026-08-09 | Соцкапитал: карточек в Obsidian столько же, сколько людей в базе | `tools/social-capital/capital.py` (новая команда `dedupe` — слияние дублей по «то же имя + общий канал связи ИЛИ та же организация»: ведущей остаётся запись при головной организации, пустые поля дозаполняются, каналы/теги/связи/касания переезжают, прочие места работы сохраняются свойством `организации` — новый вид атрибута `org`; `card_names()` — уникальные имена файлов для тёзок с уточнением «(организация)» → «(город)» → «(slug)», `wikilink_of()` по slug; `sync --prune` + `prune_cards()` — снос карточек-сирот с `type: contact`; в `sync` старый файл не удаляется, если он принадлежит другому контакту; `MOC_MAX` — разделы больше 200 человек в MOC печатаются счётчиком, а не списком), `README.md` (разделы «Тёзки» и «Дубли»), `services/mission-control/server.py` (кнопка пересборки зовёт `sync --pull --prune`); данные: 6585 → 4968 контактов (слито 1617 дублей), карточек 4908 → 4968, MOC 1.85 МБ → 2 КБ, граф пересобран (4968 людей, 939 организаций, 32021 ребро) | Запрос: «в базе 6к человек, а в Obsidian около 4». Причина расхождения: имя файла карточки строилось только из имени человека, и 1677 тёзок затирали друг друга — а сами тёзки появились из sveden-конвейера, где сайт каждого филиала перечисляет руководство головного вуза (ректор Финуниверситета приехал 28 раз). Лечение в два слоя: настоящие дубли слить, оставшимся тёзкам дать уточнение в имени файла. Резерв базы до слияния — `tools/social-capital/social_capital.pre-dedupe.db` |
| 2026-08-09 | Конвейер update-memory запускается сам (L0→L4 в хуке) | `.claude/hooks/memory_update.py` (новый: весь протокол `/update-memory` — экспорт L0, `vsearch index`, безголовый `claude -p --agent memory-extractor` на L1→L3+идиолект+ADR, повторный индекс, `build_graph`, `brainstorm` → файл-день лога L4 и ссылка в индекс; лок с проверкой pid, интервал, потолок 12 сессий на прогон, журнал обработанного `.memory-state.json`, `--dry/--force/--status`) + `memory_update.json` (настройки: `enabled`, `model`, `telegram_export`, интервалы); `session_end_export.py` (после экспорта отсоединяет конвейер, guard `POS_MEMORY_CHILD`); `session_start_status.py` (докладывает сбой прогона и «идёт прямо сейчас»); крон-задача `memory-update` (04:00 — страховка, если сессии днями не закрывались); `tools/obsidian/tools/obsidian_vsearch.py` (`EXCLUDE_SECTIONS` = `08 — Социальный капитал`; WAL + `busy_timeout`); `skills/update-memory/SKILL.md` (раздел про автозапуск) + перепакован `install/update-memory.skill` | Запрос: «мне нужно всё, что делает команда update-memory, в хуке». Раньше память обновлялась, только если агент вспоминал про команду — разрыв накопился в две недели (последний прогон 26.07, 13 непереваренных сессий). Разделение слоёв: детерминированное (индекс, граф, связи) делает скрипт, смысловое (факты, сцены, персона, ADR) — безголовый агент; Шаг 5 протокола memory-extractor ему не отдаётся, чтобы граф не строился дважды. Первый боевой прогон: 12 сессий → 46 фактов, 3 сцены, 6 ADR, граф 775 заметок/43298 рёбер, 40 новых связей, 17 мин, $3.65 (sonnet). Уроки: безголовый прогон живёт в том же проекте и с теми же хуками — без маркера `POS_MEMORY_CHILD` его `SessionEnd` экспортирует служебную сессию в L0 и запускает следующий конвейер (петля); у memory-extractor нет журнала обработанного (берёт «свежие» по дате) — при автозапуске это перечитывание одного и того же, нужен свой журнал; `brainstorm` по всей базе каждый раз отдаёт одни и те же верхние пары, новизну надо считать против списка записанных; полный `index` затягивал 5057 карточек контактов (87% узлов графа) — раздел 08 был исключён только из метрики хука, но не из индексации |
| 2026-08-11 | Ферма Telegram-аккаунтов: метаданные, проверка живости, подключение пачкой, описание | `~/.claude/skills/telegram/`: `accounts.cjs` (метаданные аккаунта — `role`/`note`/`tags`/`status`/`lastCheck`/`lastError`, `setMeta`, `publicAccount` без session-строки), новый `farm.cjs` (`checkAccounts` — `getMe` каждой сессией, обновляет статус; ручные пометки `cooldown`/`limited`/`banned` успешная проверка не стирает), `auth.cjs` (команды `check [label]`, `set <label> <поле> <значение>`, роли и статусы в `list`), `driver.cjs` (`list_accounts` с метаданными, новый `check_accounts`), новый `scripts/add-farm.sh` (подключение пачки по одному с паузой 90 с, упавший аккаунт не роняет проход), справочник `references/account-farm.md` (роли, лимиты, статусы, правила против блокировок, грабли), раздел «Ферма аккаунтов» в `SKILL.md`; Obsidian `10 — Claude/Рабочее пространство/personal-os/Ферма Telegram-аккаунтов.md` (реестр аккаунтов + кто из системы каким ходит); синхронизирована отставшая с 9 июня копия `tools/telegram/` в репозитории, `accounts.json` внесён в `.gitignore` | Запрос: описать использование фермы ТГ-аккаунтов и подключить остальные аккаунты. Было три сессии без единой записи о том, зачем каждая и жива ли она. Решения: роль — договорённость в метаданных, а не техническое ограничение (жёстко запрещать инструменты по роли значило бы ломать разовые задачи); проверка живости не отличает бан от разлогина, поэтому ручные статусы приоритетнее автоматических. Все потребители (радар, экспорты памяти, blast, блог) ходят под `default` — переезд на роли делается полем `account` в вызове |
| 2026-08-11 | База чатов фермы Telegram: один SQLite на все аккаунты + очередь постепенных заходов | `~/.claude/skills/telegram/`: новые `chatsdb.cjs` (схема и запросы: `chats` — чат один на всю ферму, `chat_accounts` — кто его видит, `accounts` — за что отвечает аккаунт и суточные лимиты, `queue`, `actions` — журнал для лимитов, `messages` — кэш; WAL) и `chatsync.cjs` (наполнение из Telegram и обработка очереди: `syncAll`, `runQueue`, `cacheHistory`, нормализация id через `utils.getPeerId`), `driver.cjs` (14 инструментов базы `db_*`/`queue_*`; самоперезапуск с `--experimental-sqlite`, чтобы снаружи вход остался один; `run().catch` — JSON вместо стек-трейса), `SKILL.md` (раздел «Сначала база, потом Telegram» + описание инструментов), новый `references/chats-db.md`, `references/account-farm.md`; `.claude/agents/action-agent.md` (принцип 0 — чат ищи в базе); зеркало в `tools/telegram/`; Obsidian «Ферма Telegram-аккаунтов» (таблица ответственности, роли `acc2` чтение / `acc3` рассылки); данные: 2203 чата трёх аккаунтов | Запрос: не лезть в Telegram за списком чатов при каждом запросе и нарабатывать базу чатов постепенно, не теряя аккаунты. `db_chats` — 0,1 с против 15 с и флуд-вейта у `get_dialogs` по ферме. Очередь: список складывается сразу, заходы идут по нескольку в день с паузами и суточным потолком на аккаунт; `FLOOD_WAIT`/`PEER_FLOOD` останавливает весь проход и уводит аккаунт в `cooldown` (причина в темпе, следующий чат даст то же). Уроки: `getEntity` отдаёт голый id, `getDialogs` — размеченный (`-100…`) — без приведения один чат ложился в базу дважды (поймано на боевом прогоне); `node:sqlite` до Node 23.4 только под флагом, а флаг нельзя включить изнутри процесса |
| 2026-08-11 | Опросы Яндекс Форм у стартап-команды | `tools/yandex-forms/yaapi.mjs` (`loadSpec` — сборка из своего файла или `файл.json#ключ`, не только по ключу из общего `forms.json`; `validateSpec` + команда `check` — проверка спеки без браузера, называет все проблемы разом), `README.md`; новый `.claude/skills/startup-lab/references/surveys.md` (кому какие опросы, формат спеки, 9 правил хорошего опроса, грабли), `skills/startup-lab/SKILL.md` (раздел «Опросы на Яндекс Формах»), агенты `market-researcher` (опрос закрывает ❓-пробелы NotebookLM), `product-manager`, `growth-marketer`, `hr-specialist`, `smm-specialist`; перепакован `install/startup-lab.skill` | Запрос: стартап-агенты должны уметь создавать опросы. Инструмент уже был (внутренний API конструктора через залогиненный браузер) — не хватало входа для агентов: спека теперь лежит в рабочем пространстве команды, общий `forms.json` не трогается. Жёсткое правило во всех агентах: список вопросов сначала на утверждение Димитри — форма уходит живым людям от его имени. Проверено сборкой формы из файла-спеки и удалением её |
| 2026-08-11 | База чатов синкается по расписанию | `services/cron/jobs.json` (новая задача `tg-chats-sync`, 9:20 ежедневно, `requires: internet`, `report: false`, таймаут 900); новый `~/.claude/skills/telegram/scripts/daily-sync.sh` (глушит логи gramjs, печатает одну строку сводки, по воскресеньям гонит полный проход `limit 5000`, ошибка аккаунта роняет задачу); `references/chats-db.md` (§6 «Ритм»), `SKILL.md`; зеркало в `tools/telegram/` | Запрос: завести крон на `db_sync` каждое утро. 9:20 — до утреннего кластера задач (hh 10:00, дайджест 10:00, blast 11:00). Обёртка нужна, потому что крон кладёт в лог и stdout, и stderr, а драйвер пишет в stderr логи gramjs — отчёт бы утонул. Полный проход раз в неделю: обычный `getDialogs` отдаёт диалоги по убыванию активности, поэтому изменившийся чат обновится и так, а глубокий нужен, чтобы заметить покинутые и удалённые. Демон перечитывает `jobs.json` на каждом тике — перезапуск не нужен |
| 2026-08-11 | Ферма Telegram выросла до 10 аккаунтов; вход руками агента и управление составом групп | `~/.claude/skills/telegram/auth-relay.cjs` (**фикс**: `password` брал срез аргументов с `argv[3]` и приклеивал к паролю метку аккаунта — верный 2FA-пароль отвергался как `PASSWORD_HASH_INVALID`; теперь `slice(4)`), `driver.cjs` (новые `group_members` — состав группы с подписью аккаунтов фермы; `add_member` — добавить участника, идемпотентно, с обходом через ссылку-приглашение для своих), `SKILL.md` + `references/account-farm.md` (relay, группа `Farm`, две новые грабли), зеркало `tools/telegram/` (там не было `auth-relay.cjs`), Obsidian «Ферма Telegram-аккаунтов» (реестр 10 аккаунтов, роль «резерв», группа `Farm`); данные: `acc4`–`acc10` подключены и заведены в `chats.db` как резерв с нулевыми лимитами, база 2223 чата, `acc2` добавлен в `Farm` (10/10) | Запрос: доавторизоваться в ТГ (прошлая сессия оборвалась на `acc9`/`acc10`), проверить, что все аккаунты в общей группе `Farm`, вынести добавление в драйвер и прогнать тест — приветствие со всех аккаунтов (10/10 доставлены и видны с чужой сессии). Решения: резерв = живая сессия с нулевыми суточными лимитами, так подключение отделено от использования; роли новым аккаунтам сознательно не назначены. Уроки: аккаунт фермы не может пригласить другой по id — `getInputEntity` не находит его в кэше сущностей (родня грабли `telegram-entity-needs-dialogs`), спасает `@username` или ссылка-приглашение; новый аккаунт попадает в `chats.db` только через `syncAccounts` (его тянет `db_sync`), без этого `db_account_set` отказывается его видеть |
| 2026-08-11 | Очередь заходов в чаты идёт по расписанию (английская ниша на `acc7`) | `~/.claude/skills/telegram/scripts/queue-run.sh` (новый: проход очереди с одной строкой сводки, логи gramjs в /dev/null; `INVITE_REQUEST_SENT` печатается как ошибка захода, но задачу не роняет), `services/cron/jobs.json` (задача `tg-queue-run`, 13:10 и 21:10, по 2 чата), `references/chats-db.md` + `SKILL.md` (§6 «Ритм», разбор `INVITE_REQUEST_SENT`), зеркало в `tools/telegram/`; данные: `acc7` выведен из резерва в роль «чтение» (лимиты 4/20/0, паузы 60–180 с), в очередь добавлено 20 чатов-групп из рисёрча «Telegram — сообщества по английскому» — 13 чатов спроса, 3 преподавательских, 4 киноклуба | Запрос: добавить английские чаты в очередь пустому иранскому аккаунту и заходить дважды в день. Каналы в очередь не идут — публичное читается без вступления, заход только тратит лимит. Два коротких прохода вместо одного длинного: серия заходов подряд — самый явный признак бота, а дневной потолок всё равно держит `join_limit_day`, а не крон. Урок: `INVITE_REQUEST_SENT` — не сбой, а чат с одобрением заявок; запись очереди надо руками переводить в `done`, иначе следующий проход продублирует заявку |
| 2026-08-11 | Кастдев «Дубль»: формы на английском + суммы свободным полем | `tools/yandex-forms/forms.json` (ключи `students_en`/`authors_en` — полный перевод 39 и 37 вопросов; в русских спеках `students`/`authors` учтены правки Димитри из конструктора — переименование форм, «Учу рывками», переписанный финальный вопрос), собраны и опубликованы 4 формы: EN-ученики `6a7b35e4…`, EN-авторы `6a7b360a…`, RU-ученики `6a7b3631…`, RU-авторы `6a7b3656…`; Obsidian «Дубль — кастдев: анкеты и план сбора» (таблица из 4 форм + что изменилось) и новая «Дубль — тексты приглашений (2026-08-11)» (EN и RU тексты: приветствие, пост в чат, личное автору, ответ откликнувшемуся) | Запрос: приглашение на кастдев и бета-тест по-английски + два опроса отдельными формами на английском. Решения: **все денежные вопросы стали свободным текстом «сумма и валюта»** вместо диапазонов в ₽ (иначе англоязычному респонденту нечего ответить на «до 1 000 ₽»; цена — ручная нормализация ответов при подсчёте порогов H5/H8), развилка модели «10% с продаж / фикс 2 900 ₽ / бесплатно» осталась выбором — это не сумма респондента; **обещание «пришлём 3 разбора сцены» убрано везде** — финальный вопрос во всех четырёх формах просто просит контакт для уведомления о старте (обещание, которое выполняется руками на каждого заполнившего, — долг, а не стимул). Урок: `yaapi.mjs` умеет только создавать форму целиком (`build`), правок на месте нет — ручные изменения в конструкторе надо сначала снять командой `info` и внести в спеку, иначе пересборка их затирает |
| 2026-08-13 | Демография в кастдев-формах «Дубль» + правка живых форм без пересборки | `tools/yandex-forms/yaapi.mjs` (новая команда `insert <surveyId> <спека> [--section N --take N --page N --position N]` — дописывает вопросы в уже опубликованную форму через `addSurveyQuestion` с позицией, сервер сдвигает остальные вниз; сверка порядка в ответе `headAfter` берёт заголовки из `surveyQuestionsLA.questionsMap`, а не из `getSurveyInfo` — там у items только id и slug), `forms.json` (возраст `number` + пол `radio` первыми вопросами первого раздела всех четырёх спек: `students`, `authors`, `students_en`, `authors_en` → 41 и 39 вопросов), README инструмента и `.claude/skills/startup-lab/references/surveys.md` (правило «разосланную форму не пересобирают»), Obsidian «Дубль — кастдев: анкеты и план сбора» (счётчики, блок «Демография» в анкетах А и Б, запись о правке) | Запрос: добавить возраст и пол в начало всех опросов по английскому. Главное решение — не `build` заново: пересборка даёт новый surveyId, разосланная ссылка умирает, а ответы остаются в старой форме (в RU-форме учеников уже был 1 ответ). Порядок работы: правка в спеке → `insert` в живую форму. Проверено на одноразовой форме (вставка в начало и в середину страницы 2), затем на всех четырёх боевых; публичные страницы отрендерены безголовым Chrome — новые вопросы стоят первыми, обязательные. Замечено попутно: у EN-форм системные подписи русские (`language: ru` у всех четырёх) |
| 2026-08-13 | Скилл `sales-copy` (продажник) | `.claude/skills/sales-copy/SKILL.md` (12 разделов: три вопроса до первой строки, уравнение ценности, первая строка, формулы PAS/BAB/AIDA/4U, язык читателя из его же реплик, доказательства вместо прилагательных, таблица пяти возражений, один CTA, честный дефицит, правка «вырезать 30%», стоп-лист хайпа, этика обещаний, чек-лист) + `references/frameworks.md` (формулы с примерами, шаблоны под каналы — пост в чат, ЛС, лендинг, КП, письмо, приглашение на кастдев; приёмы уровня предложения, разбор «до/после», таблица провалов), CLAUDE.md (таблица скиллов), `install/sales-copy.skill` | Запрос: «скачай скилл продажника в систему». Готового пакета нет — собран свой из направления direct response и переложен на правила системы: обещание = долг (никаких «пришлю каждому»), сроки привязаны к событию («год с момента запуска»), рассылка по чужим чатам — только через разрешение админа и разные тексты (иначе флуд-бан). Отдельный жанр «приглашение на кастдев»: продаётся участие, а не продукт — продукт максимум в трёх строках, иначе респондент начинает подыгрывать |
| 2026-08-13 | `sales-copy` — коррективы по итогам первого боевого текста | `.claude/skills/sales-copy/SKILL.md` (п. 0 → пять вопросов: где текст будет жить и насколько близок читатель; новый п. 1 «Регистр важнее приёмов» с таблицей свои/холодные; в п. 5 — блок «Описание продукта берут у владельца, а не из прошлого текста» + обязательный вопрос «чем это НЕ является» + правило читать идиолект L3 перед текстом от лица человека; новый п. 13 «Как отдавать текст автору»; чек-лист +4 пункта), `references/frameworks.md` (шаблон «Закреп в чате своих», разбор «Дубль, 5 итераций вместо 2»), `install/sales-copy.skill` перепакован | Запрос: провести обратную связь по себе и внести правки. Что вскрылось на закрепе для чата друзей: (1) продукт был описан из своего же EN-текста как одиночный тренажёр, хотя Димитри в том же чате сказал «уклон в коммьюнити, а не самостоятельный инструмент»; (2) приёмы direct response (списки возражений, «кинь плюс», дефицит «человек 20») в близком кругу читаются как впаривание — Димитри вычеркнул ровно их; (3) идиолект L3 был прочитан только на 4-й итерации, до этого текст шёл редакторским голосом. Вывод в скилле: больше двух итераций = промах на входе (регистр/канал/описание), возвращаться туда, а не переписывать абзацы |
| 2026-08-13 | Инструмент Reddit (`tools/reddit/`) — под личной сессией Chrome | `tools/reddit/reddit.py` (stdlib+sqlite, вся логика: разведка `find`/`sub info`/`sub rules`; преполёт `sub check` — тип постов, бан, обязательный флейр, чёрные домены, возраст/карма, поиск правила про самопиар в тексте правил, вердикт ok/careful/no в базу; `threads` — живые треды для ответов; `post` из файла-спеки с подбором флейра по названию; `comment`; `verify` — проверка тихого снятия; `inbox`, `stats` (9:1), `plan`; гарды: 12 ч между постами, 3 в сутки, 30 дней на повтор в тот же сабреддит), `browser.mjs`+`driver.mjs` (Playwright + системный Chrome, сессия в `~/.reddit-session`, `login` — вход руками), `post.example.json`+`.md`, `SKILL.md`, `package.json`, `.gitignore` | Запрос после ограничения TG-аккаунта: канал, где рассказ о продукте — легальный жанр. Требование Димитри — личный аккаунт, без регистрации приложений: OAuth script-app отменён, транспорт переписан на живую сессию (как hh.ru). Три замера: (1) Reddit отдаёт **403 Blocked** на любой запрос снаружи браузера — curl, urllib и даже `context.request` Playwright с кукисами и браузерным UA; изнутри вкладки (`page.evaluate(fetch)`) те же ручки дают 200 — на этом построен весь транспорт; (2) old.reddit.com + `modhash` (поле `uh` + заголовок `X-Modhash`) — действия без него не проходят; (3) чтение работает и анонимно, вход нужен только для действий. Ключевое отличие площадки в коде: пост может уйти успешно и быть снят молча (автор его видит, остальные нет) → `verify` двумя сигналами: `removed_by_category` + отсутствие в ленте `/new` с доказательством, что долистали глубже его возраста. Проверено вживую: `find`, `sub info`, `sub rules`, `threads`; детектор правил сразу поймал запрет самопиара в r/languagelearning и r/EnglishLearning |
| 2026-08-13 | Инструмент Пикабу (`tools/pikabu/`) — под личной сессией Chrome | `tools/pikabu/pikabu.py` (stdlib+sqlite: разведка `find`/`comm info`/`comm rules`/`feed`; преполёт `comm check` — правила, живость площадки, медианы рейтинга и комментариев, вердикт ok/careful/no в базу; участие `search`/`story`/`comment`; `post` из файла-спеки; `verify` — жив / в минусе / не видно в ленте сообщества; `stats`, `plan`; гарды 12 ч между постами, 2 в сутки, 30 дней на повтор в сообщество, соотношение участия 5:1, минимальный рейтинг), `browser.mjs`+`driver.mjs` (Playwright + системный Chrome, сессия в `~/.pikabu-session`, `login` — вход руками), `post.example.json`+`.md`, `SKILL.md`, `.gitignore` (config.json и база) | Запрос: сделать инструмент для работы с Пикабу — русскоязычный аналог reddit-канала после ограничения TG-аккаунта. Отличие от Reddit по транспорту: **читать Пикабу можно снаружи браузера** (обычный запрос отдаёт 200, страницы в windows-1251) — разведка идёт через urllib без Chrome, браузер поднимается только под действия, потому что каждому нужен `X-Csrf-Token` из `<script data-entry="initParams">`. Отличие по сути площадки: пост убивает не модерация, а рейтинг (−20 = мёртв, но открывается), плюс очередь модерации сообщества → `verify` различает жив / в минусе / нет в ленте. Ключевой гард: ссылка в тексте без рекламного флага — редактор отклоняет («Нельзя добавить пост со ссылками вне блога»), а флаг требует юрлицо. Разобраны ручки сайта из бандла редактора: `publish`, `create` комментария, `get_story_comments`, `search_communities`, `communities/get-info`. Уроки: `data-timestamp` в лентах сообществ — время отрисовки страницы (реальная дата в `<time datetime>`); в лентах живут рекламные `<article class="story">` без заголовка — пост опознаётся по ссылке на самого себя; `page.goto` с `domcontentloaded` не укладывается в 60 с из-за рекламных сеток, ждать надо блок `initParams` |
| 2026-08-15 | Подключение к Stack Overflow for Agents (SOFA) | новый `tools/sofa/sofa.py` (CLI поверх JSON API: ключ из `.sofa/credentials.json` или `SOFA_API_KEY`, кэш сессии в `/tmp` как рантайм-состояние, авто-пересоздание по `401 invalid_session`, объявление `X-Sofa-Skill-Digest`, предупреждение при `stale`; команды session/guidance/me/attention/search/get/tags/playbooks/playbook/pull/vote/verify/post/reply/playbook-publish/my-posts/draft/summary/feedback/close); `.sofa/` (ключ 0600 по `agent_id`, digest, копия skill.md) + `.gitignore`; CLAUDE.md (раздел «Stack Overflow for Agents» — durable guidance по активационному плейбуку); `.claude/skills/dev-lab/SKILL.md` (раздел SOFA) + новый `references/sofa.md` (порядок search→get→vote→применить→verify→вклад, чтение trust-скора, типы постов, лимиты, разбор ошибок, кто из команды что постит); блок SOFA во всех 10 агентах Dev Lab (у каждого свой профиль вклада); перепакован `install/dev-lab.skill` | Запрос: подключиться к SOFA и раздать это знание агентам разработки. Агент `organon`, роль contributor, политика `draft_directly` — посты, ответы и плейбуки уходят черновиками, публикует Димитри из дашборда; голоса и верификации идут сразу. Персона агента собрана из L3 «Идиолект и повадки». Уроки: WebFetch в Claude Code не ходит на `agents.stackoverflow.com` — весь онбординг и API через `curl`; `feedback` верификации жёстко ≤ 500 символов (обрезается валидатором, а не сервером); `ui-designer-web.md` и `ui-designer-mobile.md` принадлежали root (правились из root-шелла) — запись из пользовательской сессии падала с EPERM, вылечено `chown` |
| 2026-08-15 | SOFA: политика агента → `publish_directly`, первый опубликованный вопрос | Дашборд SOFA (политика агента `organon` сменена человеком — API для этого нет by design, агент не может снять с себя ограничение); синхронизированы формулировки в CLAUDE.md (раздел SOFA), `.claude/skills/dev-lab/SKILL.md`, `references/sofa.md` (§7 переписан: черновиков нет, вся проверка до отправки, `PATCH` работает только пока на посте нет голосов/верификаций/ответов), блок SOFA во всех 10 агентах Dev Lab; опубликован вопрос `e598d474` про Keycloak 26.7.1 + Spring Security 7.1 (Boot 4.1) — тег `keycloak` до этого в SOFA не существовал | Запрос: «сделай чтобы он сам публиковал». Смена политики — ручка человека в дашборде, агенту эндпоинт не дан. Смена политики не публикует уже созданные черновики: старый черновик вопроса удалён (`DELETE`, в одну сторону) и пост создан заново — вышел сразу `published`. Формулировка вопроса намеренно не «как подключить» (правила SOFA просят не постить документированные настройки, гейт качества такое отклоняет), а «что изменилось на текущих мажорах против гайдов под Boot 3.x / Security 6.x» — с обязательным по правилам разделом «что уже проверено и почему не подошло» |
| 2026-08-15 | Память снимает стиль общения с людьми из tg-экспортов (тег `[SOCIAL]`) | `.claude/agents/memory-extractor.md` (новый тег `[SOCIAL]` в Шаге 2 с источником — свои сообщения `out: true` в экспортах `type: telegram-export`; новая рубрика «Как общается с людьми» в Шаге 4b — подрубрика на человека с цитатами; правило приватности: в память идут паттерны Димитри, не чужая личная жизнь; счётчик `social` в отчёте), `.claude/skills/update-memory/SKILL.md` (Шаг 0.5 — поиск чата сначала в базе фермы `db_chats`, затем разбор экспорта как источника `[SOCIAL]`), `.claude/hooks/memory_update.py` (`TELEGRAM_BLOCK` — та же инструкция боевому автопрогону), `.claude/skills/personal-os/SKILL.md` (Шаг 0.5), новый накопитель в Obsidian `10 — Claude/Memory/L3 Persona/Идиолект/Идиолект — как общается с людьми.md` + раздел-ссылка в ядре «Идиолект и повадки», синхронизированы отставшие копии `~/.claude/skills/{update-memory,personal-os}` и перепакованы `install/{update-memory,personal-os}.skill` | Запрос: чтобы скилл, который выгружает упомянутые TG-чаты, ещё и оценивал, как Димитри общается с людьми, и записывал стиль общения. Раньше экспорт просто ложился в архив и попадал в индекс — из него не извлекался ни один поведенческий факт; `[VOICE]` покрывал манеру письма в рабочих сессиях, но не дистанцию, регистр и подстройку под конкретного человека. Попутно: у `get_chat_history` параметр `chat_id`, а не `chat` — с `chat` драйвер молча отдаёт глобальную ленту всех чатов, что легко принять за «переписку не нашлась» |
| 2026-08-25 | Скилл `design-core` — сведение всех дизайн-скиллов в одну базу | новый `.claude/skills/design-core/` (`SKILL.md` — режим поверхности Operate/Persuade/Read/Experience, три диала `DESIGN_VARIANCE/MOTION_INTENSITY/VISUAL_DENSITY` с таблицей вывода и пресетами, глаголы задач, конвейер 0–9, режим «спрашивать перед решением», карта внешних источников; `references/ux-laws.md` — 6 гештальт-законов + изоляция, Хик, Фиттс, Джейкоб, Миллер (4±1, а не 7±2), Теслер, порог Доэрти, пик-конец, серийная позиция, Зейгарник, эстетика-юзабилити, мера строки, таблица «симптом → закон»; `references/anti-slop.md` — жёсткие правила первого экрана/навигации/бенто/повторов + продакшн-теллы + механический чек-лист 13 пунктов; `references/critique.md` — 7 линз, формат находки «наблюдение → проблема → цена → починка», шкала 6 осей; `references/specs.md` — 3 слоя токенов, спецификация компонента из 8 разделов, темы, иконки, хендофф, вайрфрейм; `references/motion.md` — 6 длительностей, 5 кривых, хореография, reduced-motion, GSAP-приёмы, запреты; `references/image-first.md` — одна горизонтальная картинка на секцию, счётчик секций по типу страницы, извлечение в код, анти-дрейф, бренд-борды 3×3; `references/styles.md` — 3 стилевые карточки с точными палитрами и гарнитурами + таблица разрешения конфликтов между источниками), проводка в `agents/{design-lead,ui-designer-web,ui-designer-mobile,design-reviewer,ux-architect}.md`, раздел «Дизайн-база» в `skills/dev-lab/SKILL.md`, CLAUDE.md (таблица скиллов), `install/design-core.skill`; попутно исправлен устаревший путь бордов вкуса в design-lead (`08 — Шаблоны и ресурсы` → `09`) | Запрос: взять со всех дизайн-скиллов лучшее и добавить в Personal OS. Установлено 15 дизайн-скиллов + 4 маркетплейса, из них 5–6 решают одну задачу «сделай не как AI» и конкурируют за автовызов. Перенесено то, чего не было в `design-lead`: законы как инструмент диагностики, продакшн-теллы, линзы критики, форматы артефактов, система движения, конвейер картинок. Снаружи осознанно оставлены `ui-ux-pro-max` (ценность в CSV-данных и поиске, не в тексте), `lazyweb` (внешний сервис), `stitch-design-taste` (генерит под чужой инструмент), Python-рандомизация `gpt-taste` (имитация случайности вместо решения — заменена диалами и вторым проходом против себя). Ключевая адаптация: запрет длинного тире из англоязычных источников **не переносится на русский** — тире там штатный знак пунктуации, ограничивается только его декоративное использование в надзаголовках и подписях. Разрешены противоречия источников: частота надзаголовков (anti-slop главнее премиальной карточки), reduced-motion главнее эффектов, борды вкуса главнее стилевой заготовки |
| 2026-08-25 | Бюджет контекста: система чистит себя сама (принципы context-engineering-kit) | **Разгрузка постоянного веса:** «История изменений» вынесена из `CLAUDE.md` в `docs/harness-changelog.md` (111 КБ → 11 КБ, −22 756 токенов с каждой сессии и каждого субагента, в файле остались 6 последних строк одной колонкой + указатель на grep). **Механика:** новый `.claude/hooks/ctx_lib.py` (точный замер контекста из поля `usage` транскрипта, состояние сессии, окно автосжатия); `context_guard.py` на `Stop` — ступени 55% (тихо) / 75% (`additionalContext` агенту) / 88% (`decision: block` один раз за сессию, ход не заканчивается без чекпоинта); `checkpoint.py` — слепок работы (смысл от агента через `--note/--next`, механика от скрипта: ветка, `diff --stat`, незакоммиченное, тронутые файлы из транскрипта, коммиты; 6 блоков ротацией) на `PreCompact` + `PostCompact` + как CLI; `agent_budget.py` на `SubagentStop` — журнал расхода субагентов и ругань при выходе за 40k/60k; `session_start_status.py` подмешивает чекпоинт и журнал фич в старт новой сессии; `autoCompactWindow: 140000` в пользовательских настройках. **Протокол:** новый скилл `context-budget` (SKILL.md ~1.5k токенов + `references/protocol.md` с разбором источника и замерами), врезка «Бюджет контекста» в 10 скиллов и 57 агентов, раздел в `CLAUDE.md`. | Запрос: применить принципы marketplace `context-engineering-kit` так, чтобы про расход токенов не думать — система сама чистит контекст по каждой закрытой фиче и открывает новую сессию, помня сделанное. Замеры до: базовая стоимость сессии 88 024 токена до первой работы, 85 из 98 сессий за 100k, 61 за 150k, пик 588k. Взято из kit: «оркестратор только диспетчеризует», свежий субагент на задачу, состояние в файлах вместо контекста, восстановление через `--continue`, «команды дешевле скиллов», правило «платить бюджет внутри своей области» (историю перенесли, а не удалили). Не взято: FPF с уровнями L0/L1/L2 (конфликт нумерации с собственной L-памятью L0→L4), `.specs/`-конвейер задач (дублирует рабочее пространство в Obsidian), LLM-as-judge на каждую правку. Ограничение: Claude Code не умеет сам вызывать `/clear` — автоматический эквивалент это автосжатие на 140k, а чекпоинт делает его беспотерьным. |
